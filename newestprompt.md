Instead of showing pokemon from one generatino at a time, let's show them all, while only fetching the generation endpoints from the pokeapi

let's keep all of them saved on a variable, or even local storage to be read from there in new page visits  
(later replaced by the initial fetch) `

Now the gen buttons will work as filters, if one or more are pressed, show the pokemon from the selected generations, if none is selected, show them all
